// Small tuple helpers are used during tokenizer/parser bootstrap before the
// full iterator and slice APIs exist.

pub fn ordered(first: usize, second: usize) -> (usize, usize) {
    if first <= second {
        return -> (first, second)
    }
    return -> (second, first)
}
