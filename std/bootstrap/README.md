# Bootstrap std

Minimal Zin modules needed to compile the self-hosted compiler: memory primitives, allocator interface, slices/strings helpers, minimal formatting, Linux syscalls, ELF/file I/O and collections required by the compiler.
