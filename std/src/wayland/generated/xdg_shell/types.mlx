// Generated by std.wayland.materialize from the canonical protocol XML
// under std/protocols/wayland (protocols: wayland xdg_shell). Do not edit; regenerate with
// std/protocols/wayland/materialize.sh.

// Typed client proxies and server resources of the "xdg_shell" protocol, declared in
// dependency order.

const client = @import("../../client.mlx")
const server = @import("../../server.mlx")
const wire = @import("../../wire.mlx")
const interfaces = @import("../interfaces.mlx")
const Interface = interfaces.Interface
const wayland_types = @import("../wayland/types.mlx")

// xdg_positioner proxy (client side): one method per request.
pub const xdg_positioner_Proxy = struct {
    object: client.Object

    pub fn fromObject(object: client.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return client.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn setHandler(self: Self, handler: client.EventHandler, context: *anyopaque) -> void { client.setHandler(self.object, handler, context) }
    // Destroys the proxy locally without sending a request.
    pub fn forget(self: Self) -> void { client.forget(self.object) }

    pub fn destroy(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 0, 1, &message) { return }
        client.endRequest(self.object, &message, true)
    }

    pub fn setSize(self: Self, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 1, 1, &message) { return }
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        client.endRequest(self.object, &message, false)
    }

    pub fn setAnchorRect(self: Self, x: i32, y: i32, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 2, 1, &message) { return }
        wire.putInt(&message, x)
        wire.putInt(&message, y)
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        client.endRequest(self.object, &message, false)
    }

    pub fn setAnchor(self: Self, anchor: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 3, 1, &message) { return }
        wire.putUint(&message, anchor)
        client.endRequest(self.object, &message, false)
    }

    pub fn setGravity(self: Self, gravity: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 4, 1, &message) { return }
        wire.putUint(&message, gravity)
        client.endRequest(self.object, &message, false)
    }

    pub fn setConstraintAdjustment(self: Self, constraint_adjustment: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 5, 1, &message) { return }
        wire.putUint(&message, constraint_adjustment)
        client.endRequest(self.object, &message, false)
    }

    pub fn setOffset(self: Self, x: i32, y: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 6, 1, &message) { return }
        wire.putInt(&message, x)
        wire.putInt(&message, y)
        client.endRequest(self.object, &message, false)
    }

    pub fn setReactive(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 7, 3, &message) { return }
        client.endRequest(self.object, &message, false)
    }

    pub fn setParentSize(self: Self, parent_width: i32, parent_height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 8, 3, &message) { return }
        wire.putInt(&message, parent_width)
        wire.putInt(&message, parent_height)
        client.endRequest(self.object, &message, false)
    }

    pub fn setParentConfigure(self: Self, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 9, 3, &message) { return }
        wire.putUint(&message, serial)
        client.endRequest(self.object, &message, false)
    }

}

// xdg_toplevel proxy (client side): one method per request.
pub const xdg_toplevel_Proxy = struct {
    object: client.Object

    pub fn fromObject(object: client.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return client.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn setHandler(self: Self, handler: client.EventHandler, context: *anyopaque) -> void { client.setHandler(self.object, handler, context) }
    // Destroys the proxy locally without sending a request.
    pub fn forget(self: Self) -> void { client.forget(self.object) }

    pub fn destroy(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 0, 1, &message) { return }
        client.endRequest(self.object, &message, true)
    }

    pub fn setParent(self: Self, parent: ?*const Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 1, 1, &message) { return }
        var nullable_0: u32 = 0
        if parent != null { nullable_0 = parent.?.*.object.id }
        wire.putObject(&message, nullable_0)
        client.endRequest(self.object, &message, false)
    }

    pub fn setTitle(self: Self, title: []const u8) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 2, 1, &message) { return }
        wire.putStringSlice(&message, title)
        client.endRequest(self.object, &message, false)
    }

    pub fn setAppId(self: Self, app_id: []const u8) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 3, 1, &message) { return }
        wire.putStringSlice(&message, app_id)
        client.endRequest(self.object, &message, false)
    }

    pub fn showWindowMenu(self: Self, seat: wayland_types.wl_seat_Proxy, serial: u32, x: i32, y: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 4, 1, &message) { return }
        client.putObject(self.object, &message, seat.object)
        wire.putUint(&message, serial)
        wire.putInt(&message, x)
        wire.putInt(&message, y)
        client.endRequest(self.object, &message, false)
    }

    pub fn move(self: Self, seat: wayland_types.wl_seat_Proxy, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 5, 1, &message) { return }
        client.putObject(self.object, &message, seat.object)
        wire.putUint(&message, serial)
        client.endRequest(self.object, &message, false)
    }

    pub fn resize(self: Self, seat: wayland_types.wl_seat_Proxy, serial: u32, edges: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 6, 1, &message) { return }
        client.putObject(self.object, &message, seat.object)
        wire.putUint(&message, serial)
        wire.putUint(&message, edges)
        client.endRequest(self.object, &message, false)
    }

    pub fn setMaxSize(self: Self, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 7, 1, &message) { return }
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        client.endRequest(self.object, &message, false)
    }

    pub fn setMinSize(self: Self, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 8, 1, &message) { return }
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        client.endRequest(self.object, &message, false)
    }

    pub fn setMaximized(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 9, 1, &message) { return }
        client.endRequest(self.object, &message, false)
    }

    pub fn unsetMaximized(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 10, 1, &message) { return }
        client.endRequest(self.object, &message, false)
    }

    pub fn setFullscreen(self: Self, output: ?*const wayland_types.wl_output_Proxy) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 11, 1, &message) { return }
        var nullable_0: u32 = 0
        if output != null { nullable_0 = output.?.*.object.id }
        wire.putObject(&message, nullable_0)
        client.endRequest(self.object, &message, false)
    }

    pub fn unsetFullscreen(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 12, 1, &message) { return }
        client.endRequest(self.object, &message, false)
    }

    pub fn setMinimized(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 13, 1, &message) { return }
        client.endRequest(self.object, &message, false)
    }

}

// xdg_popup proxy (client side): one method per request.
pub const xdg_popup_Proxy = struct {
    object: client.Object

    pub fn fromObject(object: client.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return client.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn setHandler(self: Self, handler: client.EventHandler, context: *anyopaque) -> void { client.setHandler(self.object, handler, context) }
    // Destroys the proxy locally without sending a request.
    pub fn forget(self: Self) -> void { client.forget(self.object) }

    pub fn destroy(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 0, 1, &message) { return }
        client.endRequest(self.object, &message, true)
    }

    pub fn grab(self: Self, seat: wayland_types.wl_seat_Proxy, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 1, 1, &message) { return }
        client.putObject(self.object, &message, seat.object)
        wire.putUint(&message, serial)
        client.endRequest(self.object, &message, false)
    }

    pub fn reposition(self: Self, positioner: xdg_positioner_Proxy, token: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 2, 3, &message) { return }
        client.putObject(self.object, &message, positioner.object)
        wire.putUint(&message, token)
        client.endRequest(self.object, &message, false)
    }

}

// xdg_surface proxy (client side): one method per request.
pub const xdg_surface_Proxy = struct {
    object: client.Object

    pub fn fromObject(object: client.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return client.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn setHandler(self: Self, handler: client.EventHandler, context: *anyopaque) -> void { client.setHandler(self.object, handler, context) }
    // Destroys the proxy locally without sending a request.
    pub fn forget(self: Self) -> void { client.forget(self.object) }

    pub fn destroy(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 0, 1, &message) { return }
        client.endRequest(self.object, &message, true)
    }

    pub fn getToplevel(self: Self) -> xdg_toplevel_Proxy {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 1, 1, &message) { return xdg_toplevel_Proxy.fromObject(client.nullObject(self.object)) }
        const created = client.createObject(self.object, Interface.xdg_toplevel, client.objectVersion(self.object))
        wire.putNewId(&message, created.id)
        client.endRequest(self.object, &message, false)
        return xdg_toplevel_Proxy.fromObject(created)
    }

    pub fn getPopup(self: Self, parent: ?*const Self, positioner: xdg_positioner_Proxy) -> xdg_popup_Proxy {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 2, 1, &message) { return xdg_popup_Proxy.fromObject(client.nullObject(self.object)) }
        const created = client.createObject(self.object, Interface.xdg_popup, client.objectVersion(self.object))
        wire.putNewId(&message, created.id)
        var nullable_1: u32 = 0
        if parent != null { nullable_1 = parent.?.*.object.id }
        wire.putObject(&message, nullable_1)
        client.putObject(self.object, &message, positioner.object)
        client.endRequest(self.object, &message, false)
        return xdg_popup_Proxy.fromObject(created)
    }

    pub fn setWindowGeometry(self: Self, x: i32, y: i32, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 3, 1, &message) { return }
        wire.putInt(&message, x)
        wire.putInt(&message, y)
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        client.endRequest(self.object, &message, false)
    }

    pub fn ackConfigure(self: Self, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 4, 1, &message) { return }
        wire.putUint(&message, serial)
        client.endRequest(self.object, &message, false)
    }

}

// xdg_wm_base proxy (client side): one method per request.
pub const xdg_wm_base_Proxy = struct {
    object: client.Object

    pub fn fromObject(object: client.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return client.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn setHandler(self: Self, handler: client.EventHandler, context: *anyopaque) -> void { client.setHandler(self.object, handler, context) }
    // Destroys the proxy locally without sending a request.
    pub fn forget(self: Self) -> void { client.forget(self.object) }

    pub fn destroy(self: Self) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 0, 1, &message) { return }
        client.endRequest(self.object, &message, true)
    }

    pub fn createPositioner(self: Self) -> xdg_positioner_Proxy {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 1, 1, &message) { return xdg_positioner_Proxy.fromObject(client.nullObject(self.object)) }
        const created = client.createObject(self.object, Interface.xdg_positioner, client.objectVersion(self.object))
        wire.putNewId(&message, created.id)
        client.endRequest(self.object, &message, false)
        return xdg_positioner_Proxy.fromObject(created)
    }

    pub fn getXdgSurface(self: Self, surface: wayland_types.wl_surface_Proxy) -> xdg_surface_Proxy {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 2, 1, &message) { return xdg_surface_Proxy.fromObject(client.nullObject(self.object)) }
        const created = client.createObject(self.object, Interface.xdg_surface, client.objectVersion(self.object))
        wire.putNewId(&message, created.id)
        client.putObject(self.object, &message, surface.object)
        client.endRequest(self.object, &message, false)
        return xdg_surface_Proxy.fromObject(created)
    }

    pub fn pong(self: Self, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !client.beginRequest(self.object, 3, 1, &message) { return }
        wire.putUint(&message, serial)
        client.endRequest(self.object, &message, false)
    }

}

// xdg_wm_base resource (server side): one method per event.
pub const xdg_wm_base_Resource = struct {
    object: server.Object

    pub fn fromObject(object: server.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return server.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn client(self: Self) -> server.Client { return self.object.client }
    pub fn setHandler(self: Self, handler: server.RequestHandler, context: *anyopaque) -> void { server.setHandler(self.object, handler, context) }
    pub fn setDestroyHandler(self: Self, handler: server.DestroyHandler, context: *anyopaque) -> void { server.setDestroyHandler(self.object, handler, context) }
    pub fn postError(self: Self, code: u32, message: []const u8) -> void { server.postError(self.object, code, message) }
    pub fn destroy(self: Self) -> void { server.destroyObject(self.object) }

    pub fn sendPing(self: Self, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 0, 1, &message) { return }
        wire.putUint(&message, serial)
        server.endEvent(self.object, &message, false)
    }

}

// xdg_positioner resource (server side): one method per event.
pub const xdg_positioner_Resource = struct {
    object: server.Object

    pub fn fromObject(object: server.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return server.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn client(self: Self) -> server.Client { return self.object.client }
    pub fn setHandler(self: Self, handler: server.RequestHandler, context: *anyopaque) -> void { server.setHandler(self.object, handler, context) }
    pub fn setDestroyHandler(self: Self, handler: server.DestroyHandler, context: *anyopaque) -> void { server.setDestroyHandler(self.object, handler, context) }
    pub fn postError(self: Self, code: u32, message: []const u8) -> void { server.postError(self.object, code, message) }
    pub fn destroy(self: Self) -> void { server.destroyObject(self.object) }

}

// xdg_surface resource (server side): one method per event.
pub const xdg_surface_Resource = struct {
    object: server.Object

    pub fn fromObject(object: server.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return server.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn client(self: Self) -> server.Client { return self.object.client }
    pub fn setHandler(self: Self, handler: server.RequestHandler, context: *anyopaque) -> void { server.setHandler(self.object, handler, context) }
    pub fn setDestroyHandler(self: Self, handler: server.DestroyHandler, context: *anyopaque) -> void { server.setDestroyHandler(self.object, handler, context) }
    pub fn postError(self: Self, code: u32, message: []const u8) -> void { server.postError(self.object, code, message) }
    pub fn destroy(self: Self) -> void { server.destroyObject(self.object) }

    pub fn sendConfigure(self: Self, serial: u32) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 0, 1, &message) { return }
        wire.putUint(&message, serial)
        server.endEvent(self.object, &message, false)
    }

}

// xdg_toplevel resource (server side): one method per event.
pub const xdg_toplevel_Resource = struct {
    object: server.Object

    pub fn fromObject(object: server.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return server.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn client(self: Self) -> server.Client { return self.object.client }
    pub fn setHandler(self: Self, handler: server.RequestHandler, context: *anyopaque) -> void { server.setHandler(self.object, handler, context) }
    pub fn setDestroyHandler(self: Self, handler: server.DestroyHandler, context: *anyopaque) -> void { server.setDestroyHandler(self.object, handler, context) }
    pub fn postError(self: Self, code: u32, message: []const u8) -> void { server.postError(self.object, code, message) }
    pub fn destroy(self: Self) -> void { server.destroyObject(self.object) }

    pub fn sendConfigure(self: Self, width: i32, height: i32, states: wire.ArrayView) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 0, 1, &message) { return }
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        wire.putArray(&message, states)
        server.endEvent(self.object, &message, false)
    }

    pub fn sendClose(self: Self) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 1, 1, &message) { return }
        server.endEvent(self.object, &message, false)
    }

    pub fn sendConfigureBounds(self: Self, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 2, 4, &message) { return }
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        server.endEvent(self.object, &message, false)
    }

    pub fn sendWmCapabilities(self: Self, capabilities: wire.ArrayView) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 3, 5, &message) { return }
        wire.putArray(&message, capabilities)
        server.endEvent(self.object, &message, false)
    }

}

// xdg_popup resource (server side): one method per event.
pub const xdg_popup_Resource = struct {
    object: server.Object

    pub fn fromObject(object: server.Object) -> Self { return Self.{ .object = object } }
    pub fn id(self: Self) -> u32 { return self.object.id }
    pub fn version(self: Self) -> u32 { return server.objectVersion(self.object) }
    pub fn isNull(self: Self) -> bool { return self.object.id == 0 }
    pub fn client(self: Self) -> server.Client { return self.object.client }
    pub fn setHandler(self: Self, handler: server.RequestHandler, context: *anyopaque) -> void { server.setHandler(self.object, handler, context) }
    pub fn setDestroyHandler(self: Self, handler: server.DestroyHandler, context: *anyopaque) -> void { server.setDestroyHandler(self.object, handler, context) }
    pub fn postError(self: Self, code: u32, message: []const u8) -> void { server.postError(self.object, code, message) }
    pub fn destroy(self: Self) -> void { server.destroyObject(self.object) }

    pub fn sendConfigure(self: Self, x: i32, y: i32, width: i32, height: i32) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 0, 1, &message) { return }
        wire.putInt(&message, x)
        wire.putInt(&message, y)
        wire.putInt(&message, width)
        wire.putInt(&message, height)
        server.endEvent(self.object, &message, false)
    }

    pub fn sendPopupDone(self: Self) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 1, 1, &message) { return }
        server.endEvent(self.object, &message, false)
    }

    pub fn sendRepositioned(self: Self, token: u32) -> void {
        var message: wire.Writer = undefined
        if !server.beginEvent(self.object, 2, 3, &message) { return }
        wire.putUint(&message, token)
        server.endEvent(self.object, &message, false)
    }

}

