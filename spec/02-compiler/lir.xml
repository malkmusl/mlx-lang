<ZinLIR version="1.0" normative="true">
  <Model>Typed SSA-capable linear intermediate representation with explicit basic blocks. Bootstrap compiler may initially lower phi nodes through stack temporaries, but canonical semantics remain SSA-compatible.</Model>
  <Types>iN f32 f64 ptr array slice tuple struct enum union vector void</Types>
  <Instructions>
    const.i const.f copy
    add sub mul div rem add_wrap sub_wrap mul_wrap add_sat sub_sat mul_sat
    and or xor not shl_checked shr_checked shl_count_wrap shr_count_wrap shl_sat shl_count_wrap_sat
    icmp fcmp
    load store addr gep
    cast bitcast ptrtoint inttoptr
    alloca
    extract insert
    vector_splat vector_extract vector_insert vector_shuffle vector_reduce vector_select
    call call_indirect
    ret ret_error
    br condbr switch phi
    optional_pack optional_test optional_unwrap
    union_tag union_payload
    error_test error_payload
    atomic_load atomic_store atomic_rmw cmpxchg fence
    asm trap unreachable
      shift_combine
  </Instructions>
  <ShiftCombine>
    `shift_combine` carries direction, count_policy, result_policy, combiner_kind, combiner_policy and combiner_position.
    Lowering evaluates source and count once, performs the selected shift, then combines original and shifted values.
  </ShiftCombine>
  <Memory>Loads/stores carry alignment and volatile flags. Atomics carry memory order.</Memory>
  <Calls>Call nodes explicitly carry calling convention, argument classes, return classes, and error-union status after ABI lowering.</Calls>
  <Defer>Defer/errdefer do not survive into final LIR; Sema/lowering expands cleanup control flow before backend lowering.</Defer>
</ZinLIR>
