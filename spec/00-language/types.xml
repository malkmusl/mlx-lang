<ZinTypeSystem version="1.0" normative="true">
  <PrimitiveIntegers signed="iN isize" unsigned="uN usize" arbitraryWidth="true" minBits="1" maxBits="4096">
    <Aliases>i8 i16 i32 i64 i128 u8 u16 u32 u64 u128 are ordinary iN/uN spellings with those widths.</Aliases>
    <Rule>No implicit integer promotion changes bit width.</Rule>
    <Rule>@bitSizeOf(iN|uN) = N; standalone @sizeOf = ceil(N/8); packed fields occupy exactly N bits.</Rule>
  </PrimitiveIntegers>
  <Floats>f32 f64</Floats>
  <Bool sizeBytes="1" falseBits="0" trueBits="1">Other bit patterns are invalid bool values.</Bool>
  <ComptimeTypes>comptime_int, comptime_float, type</ComptimeTypes>
  <SpecialTypes>void, noreturn, anytype, anyopaque</SpecialTypes>
  <Literals><Integer default="comptime_int"/><Float default="comptime_float"/></Literals>
  <Conversions>
    <Rule>No implicit runtime numeric narrowing.</Rule>
    <Rule>A comptime integer or float may coerce when exactly representable or when the target conversion rule permits rounding for floats.</Rule>
    <Rule>*T may coerce to *const T; const may never be implicitly removed.</Rule>
    <Builtins>@intCast @floatCast @floatFromInt @intFromFloat @ptrCast @alignCast @bitCast @intFromPtr @ptrFromInt @intFromEnum @enumFromInt</Builtins>
  </Conversions>
  <Arrays representation="inline value" syntax="[N]T" inferred="[_]T" boundsCheckedSafe="true"/>
  <Slices syntax="[]T | []const T" representation="pointer,length" allocation="none"/>
  <Pointers single="*T" const="*const T" optional="?*T" many="[*]T" sentinelMany="[*:sentinel]T" arithmetic="many-item only"/>
  <Optionals syntax="?T" absent="null" unwrap=".?" fallback="orelse" optionalPointerUsesNullNiche="true"/>
  <Tuples syntax=".{a,b}" indexing="[constant-index]" nativeMultipleReturns="true"/>
  <CopySemantics>
    <Rule>Values are copied by value. There are no implicit copy constructors, move constructors, or destructors.</Rule>
    <Rule>Copying a pointer copies the address. Copying a slice copies its pointer and length, not referenced data.</Rule>
  </CopySemantics>
  <EvaluationOrder>Subexpressions and function arguments evaluate left-to-right, except explicit short-circuit operators.</EvaluationOrder>
  <ShortCircuit and="evaluate RHS only when LHS true" or="evaluate RHS only when LHS false"/>
</ZinTypeSystem>
