<MlxModules version="1.0" normative="true">
  <ModuleIdentity>Each source file is a module namespace.</ModuleIdentity>
  <Import builtin="@import" relative="./parser.mlx" package="name" std="std.*" builtinModule="builtin"/>
  <Visibility default="private" publicKeyword="pub"/>
  <GlobalInitialization>Global const and var initializers must be comptime evaluable. There are no hidden runtime global constructors.</GlobalInitialization>
  <Destructors>There are no general destructors. @noncopy aggregates may opt into lexical automatic deinitialization through fn deinit(self: *Self) -> void; all other cleanup remains explicit.</Destructors>
  <CircularImports>Allowed only if declaration resolution does not require cyclic CTFE evaluation.</CircularImports>
  <LanguageVersion builtin="@languageVersion" cli="-Dlanguage=1.0"/>
</MlxModules>
