<ZinStdFmt version="1.0" normative="true">
  <PrintSignature><![CDATA[fn print(writer: anytype, comptime format: []const u8, args: anytype) !void]]></PrintSignature>
  <Specifiers default="{}" decimal="{d}" lowerHex="{x}" upperHex="{X}" binary="{b}" octal="{o}" char="{c}" string="{s}" float="{f}" scientific="{e}" pointer="{p}"/>
  <Escapes open="{{" close="}}"/>
  <Width>{d:8}</Width><ZeroPad>{d:08}</ZeroPad><Precision>{f:.2}</Precision><Combined>{f:8.2}</Combined>
  <Validation>Format strings are parsed at comptime. Arity/type/specifier mismatches are compile errors.</Validation>
  <CustomFormatting><![CDATA[pub fn format(self: Self, writer: anytype, comptime spec: []const u8) !void]]></CustomFormatting>
  <Detection>@hasDecl(T,"format")</Detection>
</ZinStdFmt>
