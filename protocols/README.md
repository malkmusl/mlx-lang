# Protocol sources

Protocol source data used to construct standard-library modules belongs to the toolchain/stdlib source process, not ordinary Mlx project configuration. Wayland's canonical XML is handled under `std/protocols/wayland` during Stage-0/1 stdlib bootstrap.
