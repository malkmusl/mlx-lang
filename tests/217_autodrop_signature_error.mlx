// Invalid automatic deinitializers are rejected with MLX-E6006.
const Broken = @noncopy(struct) {
    value: u8

    fn deinit(self: Self) -> u8 {
        return self.value
    }
}

fn main() -> u8 {
    return 0
}
