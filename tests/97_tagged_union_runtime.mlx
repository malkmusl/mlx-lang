const Event = union(enum(u8)) {
    none,
    number: u8
}

fn main() u8 {
    const event = Event.{ .number = 42 }
    const tag = @tagOf(event)
    return match event {
        .none => 1,
        .number => event.number + tag
    }
}
