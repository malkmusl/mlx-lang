const std = @import("std")

const Profile = struct {
    name: []const u8
    enabled: bool
}

pub fn main() -> !void {
    const profile = Profile.{ .name = "bob", .enabled = true }
    try std.io.printFmt("{any}\n", .{profile})
}
