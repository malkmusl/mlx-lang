const fmt = @import("std.fmt")
const io = @import("std.io")

pub fn main() -> !void {
    const values = [2]fmt.Argument{
        fmt.signed(-9),
        fmt.boolean(false),
    }
    try fmt.print(io.stdout(), "{d:04} {s}\n", values[0..2])
}
