// Tests pointer capture and dereference by reading the last array element through item.*; returns 8.
pub fn main() u8 {
    var items = [_]u8{4, 8}
    var result: u8 = 0
    for *item in items {
        result = item.*
    }
    return result
}
