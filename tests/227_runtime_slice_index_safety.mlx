// Safe builds preserve and check the length transported with a slice argument.
noinline fn readAt(values: []const u8, index: usize) -> u8 {
    return values[index]
}

fn main() -> u8 {
    const values = [_]u8{ 17, 29 }
    return readAt(values[0..], 2)
}
