// Tests error-union ABI transport for f64 by comparing the unwrapped value's IEEE-754 bit pattern.
fn source() !f64 {
    return 1.5
}

fn forward() !f64 {
    return source()
}

pub fn main() !void {
    const value = try forward()
    while @bitCast(u64, value) != 4609434218613702656 {}
}
