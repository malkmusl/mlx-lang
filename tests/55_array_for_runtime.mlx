// Tests iteration over a fixed array by summing 2, 3, and 4 to produce 9.
pub fn main() u8 {
    const items = [_]u8{2, 3, 4}
    var sum: u8 = 0
    for item in items {
        sum = sum + item
    }
    return sum
}
