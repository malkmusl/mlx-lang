// Tests array element layout and iteration order by constructing the decimal result 234.
pub fn main() u8 {
    const items = [_]u8{2, 3, 4}
    var code: u8 = 0
    for item in items {
        code = code * 10 + item
    }
    return code
}
