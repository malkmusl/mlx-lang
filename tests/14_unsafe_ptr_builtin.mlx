// Tests the unsafe boundary by converting an integer to a pointer outside unsafe; compilation must emit ZIN-E7004.
pub fn main() usize {
    const ptr = @ptrFromInt(*u8, 1)
    return @intFromPtr(ptr)
}
