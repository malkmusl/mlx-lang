// Tests pointer alignment validation with the invalid alignment 3; compilation must emit ZIN-E7002.
pub fn main() usize {
    unsafe {
        const ptr = @ptrFromInt(*align(3) u8, 1)
        return @intFromPtr(ptr)
    }
}
