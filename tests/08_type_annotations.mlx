// Tests explicit i32 annotations and typed function calls; main returns 5 + 10 = 15.
fn add(a: i32, b: i32) i32 {
    return a + b
}

fn main() i32 {
    const x: i32 = 5
    const y: i32 = 10
    const z: i32 = add(x, y)
    return z
}
