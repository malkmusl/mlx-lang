// Tests an allowed unsafe pointer round trip by converting address 1 to a pointer and back to usize.
pub fn main() usize {
    unsafe {
        const ptr = @ptrFromInt(*align(8) volatile u8, 1)
        return @intFromPtr(ptr)
    }
}
