// Tests logical precedence and short-circuit evaluation; true || (...) selects the branch returning 1.
pub fn main() u8 {
    if true || false && false {
        return 1
    } else {
        return 2
    }
}
