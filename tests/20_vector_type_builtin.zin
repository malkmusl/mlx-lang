// Tests vector type construction and layout reflection; main returns the vector size plus its bit size.
const Bytes = @Vector(4, u8)

pub fn main() u8 {
    return @sizeOf(Bytes) + @bitSizeOf(Bytes)
}
