// Tests that a @nocopy value cannot escape as a plain u8; the attempted copy must be rejected.
const OwnedByte = @nocopy(u8)

pub fn main() u8 {
    const owned: OwnedByte = 1
    @print("Test")
    const escaped: u8 = owned
    return escaped
}
