const std = @import("std")

pub fn main() -> !void {
    const values = [3]std.fmt.Argument{
        std.fmt.ownedString(std.string.fromSlice("format")),
        std.fmt.unsigned(15),
        std.fmt.pointer(255),
    }
    try std.io.printFmt("{s} {o:4} {p}\n", values[0..3])
}
