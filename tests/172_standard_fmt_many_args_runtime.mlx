const std = @import("std")

pub fn main() -> !void {
    try std.io.printFmt("{d} {s} {d} {s} {d} {s} {d} {s}\n", .{
        1,
        "two",
        3,
        "four",
        5,
        "six",
        7,
        "eight",
    })
}
