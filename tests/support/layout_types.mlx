// Aggregates for 250: @offsetOf on a type imported from another module.
pub const Extent = struct { width: u32, height: u32, depth: u32 }
pub const Tagged = struct { kind: u32, next: usize, extent: Extent, flags: u8 }
