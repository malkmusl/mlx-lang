const linux = @import("../std/bootstrap/os/linux.mlx")

pub fn main() u8 {
    // A null parameter block is invalid. A negative kernel result verifies the
    // raw gateway without creating a ring or depending on host policy.
    if linux.event.ioUringSetup(1, 0) >= 0 { return 1 }
    return 13
}
