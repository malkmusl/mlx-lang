const bootstrap = @import("../std/bootstrap/root.mlx")

pub fn main() -> !void {
    const values = [1]bootstrap.fmt.Argument{
        bootstrap.fmt.unsigned(1),
    }
    return bootstrap.fmt.print(bootstrap.io.stdout(), "{q}", values[0..1])
}
