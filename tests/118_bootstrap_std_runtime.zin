const bootstrap = @import("../std/bootstrap/root.zin")

fn main() -> u8 {
    const bounds = bootstrap.ranges.ordered(9, 4)
    if bootstrap.ascii.isDigit(53) {
        return @intCast(u8, bounds.0 + bounds.1)
    }
    return 1
}
