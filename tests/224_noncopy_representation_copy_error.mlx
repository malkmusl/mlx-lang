// Ownership metadata cannot be discarded by assigning to the wrapped representation.
const OwnedByte = @noncopy(u8)

fn main() -> u8 {
    const owned: OwnedByte = 1
    const escaped: u8 = owned
    return escaped
}
