// Tests tagged-union discriminator and payload layout by returning their combined reflected values.
const Event = union(enum(u8)) {
    none
    data: u32
}

pub fn main() u8 {
    return @sizeOf(Event) + @alignOf(Event) + @offsetOf(Event, "data") + @fieldCount(Event)
}
