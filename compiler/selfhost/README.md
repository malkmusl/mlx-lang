# Canonical self-hosted Zin compiler

This directory becomes the canonical Zin compiler written entirely in Zin. `zin0` must compile it; then it must compile itself. Do not add Zig dependencies here.
