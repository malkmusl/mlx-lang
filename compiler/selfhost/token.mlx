const implementation = @import("./lexer/token.zin")

pub const Kind = implementation.Kind
pub const Token = implementation.Token

pub fn init(kind: Kind, start: usize, length: usize) -> Token {
    return implementation.init(kind, start, length)
}
